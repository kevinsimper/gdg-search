import { LitElement, html } from 'lit-element'

class SearchEventsGraphql extends LitElement {
  render() {
    return html`Search Events GraphQL`
  }
}

customElements.define('x-search-events-graphql', SearchEventsGraphql)
